<%= form_for [:admin, @nugget] do |f| %>
  <% if @nugget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nugget.errors.count, "error") %> prohibited this nugget from being saved:</h2>

      <ul>
      <% @nugget.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body, style:"min-height: 400px;" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "button" %>
  </div>
<% end %>
